# Visualizing Emotional State through Immersive Generated Environments

## Running

1. In a terminal, `cd` into `ECS272-Final-Project-VR`.
2. Install Node packages.
   ```
   npm install
   ```
3. Start the application:
   ```
   npm run dev
   ```
4. In a web browser, navigate to `localhost:3000`
